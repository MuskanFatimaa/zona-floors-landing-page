# Server-only secrets. Copy to .env and fill in. .env is gitignored.

# CRM lead destination (GHL inbound webhook). Read server-side in
# src/pages/api/submit-lead.ts only — never exposed to the client.
CRM_WEBHOOK_URL="https://services.leadconnectorhq.com/hooks/<location>/webhook-trigger/<id>"
